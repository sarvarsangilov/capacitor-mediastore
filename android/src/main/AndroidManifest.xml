<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Android 10–12 (API 29–32): READ_EXTERNAL_STORAGE.
      На API 33+ это разрешение игнорируется системой, поэтому ограничиваем
      через maxSdkVersion, чтобы Play Console не помечал его как лишнее.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ (API 33): гранулярные media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Android 14+ (API 34): частичный доступ к визуальным медиа -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

</manifest>
